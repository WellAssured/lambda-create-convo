{
  "name": "lambda-create-convo",
  "version": "1.0.0",
  "description": "Lambda function to create a new Conversation after a Cognito New User trigger.",
  "main": "index.js",
  "scripts": {
    "zip": "zip -r ../lambda-create-convo.zip index.js package.json node_modules"
  },
  "author": "Jack White",
  "license": "ISC",
  "dependencies": {
    "amazon-cognito-identity-js": "^3.0.3",
    "node-fetch": "^2.3.0"
  }
}
